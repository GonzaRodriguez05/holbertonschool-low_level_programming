#include "main.h"
/**
 * _islower - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int _isalpha(int c)
{
if (c >= 'a' && c <= 'z')
if (c >= 'A' && c <= 'Z')

return (1);
return (0);
}
